type TProjects = {
  id: number;
  title: string;
  skills: string;
  img: string;
  bigImg: string;
  background: string;
  gitHubLink: string;
  liveDemo: string;
};

// eslint-disable-next-line import/prefer-default-export
export type { TProjects };
